import {NextResponse} from "next/server"; import {assertSupplier} from "../../../../../lib/dropshipping/supplier";
export async function POST(request:Request){try{const{supplier,externalId,items,shipping}=await request.json();const s=assertSupplier(String(supplier));if(!externalId||!Array.isArray(items)||!shipping)return NextResponse.json({error:"supplier, externalId, items and shipping are required"},{status:400});return NextResponse.json({ok:false,supplier:s,message:"Connect supplier credentials to enable automated fulfillment."},{status:501});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Invalid request"},{status:400});}}
