"use client"; import {useState} from "react"; export function Newsletter09(){const[e,setE]=useState("");const[m,setM]=useState("");return <form className="liquid-newsletter" onSubmit={x=>{x.preventDefault();setM("You're on the list.")}}><h3>Stay in the loop.</h3><input required type="email" value={e} onChange={x=>setE(x.target.value)} placeholder="Email address"/><button>Join</button>{m&&<small>{m}</small>}</form>}
