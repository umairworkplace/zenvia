"use client";
import {useState} from "react";
export function GlassAIWidget(){const[q,setQ]=useState("");const[busy,setBusy]=useState(false);const[answer,setAnswer]=useState("");async function ask(){if(!q.trim())return;setBusy(true);try{const r=await fetch("/api/ai/recommend",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:q})});const d=await r.json();setAnswer(d.answer??"Try telling me what you are shopping for.");}finally{setBusy(false)}}return <section className="glass-ai-widget"><span className="eyebrow">ZEN AI</span><h3>Your personal shopping guide.</h3><div className="ai-row"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="What are you looking for?"/><button className="glass-button" onClick={ask} disabled={busy}>{busy?"Thinking…":"Ask"}</button></div>{answer&&<p>{answer}</p>}</section>}
