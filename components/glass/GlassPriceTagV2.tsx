export function GlassPriceTagV2({price,oldPrice}:{price:number;oldPrice?:number}){return <div className="glass-price"><strong>${price.toFixed(2)}</strong>{oldPrice&&<del>${oldPrice.toFixed(2)}</del>}</div>}
