export function GlassCheckoutSummaryV2({subtotal,shipping,total}:{subtotal:number;shipping:number;total:number}){return <aside className="glass-checkout-summary"><p>Subtotal <span>${subtotal.toFixed(2)}</span></p><p>Delivery <span>{shipping?`$${shipping.toFixed(2)}`:"Free"}</span></p><hr/><h3>Total <span>${total.toFixed(2)}</span></h3><button className="glass-button">Secure checkout</button></aside>}
