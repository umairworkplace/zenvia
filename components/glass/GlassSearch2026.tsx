"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export function GlassSearch2026(){const[q,setQ]=useState("");const router=useRouter();return <form className="glass-search" onSubmit={e=>{e.preventDefault();router.push(`/shop?q=${encodeURIComponent(q)}`)}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search the marketplace…"/><button aria-label="Search">⌕</button></form>}
