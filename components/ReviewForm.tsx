"use client";
import {useState} from "react";
export function ReviewForm({productId}:{productId:string}){const[rating,setRating]=useState(5);const[body,setBody]=useState("");const[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/reviews",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({productId,rating,title:"Customer review",body})});setMessage(r.ok?"Thanks for your review.":"Sign in and complete the review setup first.");}return <form className="review-form" onSubmit={submit}><select value={rating} onChange={e=>setRating(Number(e.target.value))}>{[5,4,3,2,1].map(n=><option key={n} value={n}>{n} stars</option>)}</select><textarea required value={body} onChange={e=>setBody(e.target.value)} placeholder="Share your experience…"/><button className="primary">Submit review</button>{message&&<small>{message}</small>}</form>}
